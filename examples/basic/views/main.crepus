div flex flex-col gap-6 p-8
  span text-3xl font-bold
    "{headline}"
  span text-base
    "{detail}"
  div flex flex-row gap-2
    span text-sm font-semibold text-green-500
      "{badge}"
    span text-sm text-zinc-500
      "{sessionOwner}: {sessionMode}"
